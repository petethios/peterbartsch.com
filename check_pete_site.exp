#!/usr/bin/expect -f
set timeout 10
set sftp_host "peterbartsch.com"
set sftp_user "tronicadmin"
set sftp_pass {X!!RBeHV}

spawn ssh ${sftp_user}@${sftp_host} "ls -la petebartsch.com"
expect {
    -re ".*assword:" { 
        send "${sftp_pass}\r"
        exp_continue
    }
}
expect eof
